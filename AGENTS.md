runtime state is canonical
projections are rebuildable artifacts
prefer rg
run targeted tests after edits
don't touch draft briefs unless asked
avoid editing output/

Scoped guidance lives in `.cursor/rules/` (runtime-state, high-risk-files, briefs-and-output, commit-hygiene).
Deeper repo brain (architecture, canonical truths, testing bands, working norms) is in `CODEX.md` — read it when context calls for it.
